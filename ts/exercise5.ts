/**
 * Принимает массив слов и возвращает массив массивов-пар, где каждая пара слов - это слова-анаграммы друг друга
 * @param array массив слов
 * @returns массив анаграмм
 */
export function createAnagramArray(array: string[]): [string[]] {
  return [[]];
}