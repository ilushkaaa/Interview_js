/**
 * Принимает 2 отсортированных массива, возвращает число, которое будет посередине, если объединить эти массивы в один отсортированный массив
 * @param arr1 первый отсортированный массив
 * @param arr2 второй отсортированный массив
 * @returns медиана
 */
export function getMedian(arr1: number[], arr2: number[]): number {
  return 0;
}