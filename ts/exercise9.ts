/**
 * Возвращает массив чисел, начиная с позиции start и длинной count (Без использование циклов)
 * @param start первое число
 * @param count количество чисел
 * @returns массив чисел
 */
export function getRange(start: number, count: number): number[] {
  return [];
}